/// <reference types="react-scripts" />

interface Navigator {
  brave?: {
    isBrave: () => Promise<boolean | undefined>
  }
}
interface Navigator {
  brave?: {
    isBrave: () => Promise<boolean | undefined>
  }
}